---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "claude_memory_store Resource - claude"
subcategory: ""
description: |-
  A memory store: a workspace-scoped, named container for agent memories, mounted into sessions to persist state across runs. terraform destroy permanently deletes the store and all of its memories (falling back to archive if it is still referenced).
---

# claude_memory_store (Resource)

A memory store: a workspace-scoped, named container for agent memories, mounted into sessions to persist state across runs. `terraform destroy` permanently deletes the store and all of its memories (falling back to archive if it is still referenced).

## Example Usage

```terraform
resource "claude_memory_store" "prefs" {
  name        = "User Preferences"
  description = "Per-user preferences and project context."
  metadata = {
    external_user_id = "usr_abc123"
  }
}

# Seed the store with read-only reference material before any agent runs.
resource "claude_memory" "formatting_standards" {
  memory_store_id = claude_memory_store.prefs.id
  path            = "/formatting_standards.md"
  content         = <<-EOT
    All reports use GAAP formatting.
    Dates are ISO-8601.
  EOT
}

output "memory_store_id" {
  value = claude_memory_store.prefs.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable name (1-255 chars). The `/mnt/memory/` mount slug is derived from this.

### Optional

- `description` (String) What the store contains (up to 1024 chars). Included in the agent's system prompt when attached.
- `metadata` (Map of String) Arbitrary key-value tags (max 16; not visible to the agent).

### Read-Only

- `archived_at` (String) Archive timestamp (RFC 3339), or null if active.
- `created_at` (String)
- `id` (String) The ID of this resource.
- `type` (String)
- `updated_at` (String)
